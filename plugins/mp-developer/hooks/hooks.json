{
  "description": "Prevents Mercado Pago credentials from being written to source files and blocks reading .env files to prevent credential exposure",
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/validate_mp_credentials.py",
            "timeout": 10
          }
        ],
        "matcher": "Bash|Edit|Write|MultiEdit|Read"
      }
    ]
  }
}
