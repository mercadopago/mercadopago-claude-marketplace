# Server-side Checkout Pro credential. Use a test collector for local validation;
# never expose this value to the browser, the model, logs, or version control.
MERCADOPAGO_ACCESS_TOKEN=

# The library reads currency, price, title, and stock from the trusted catalog.
# Do not configure model-controlled prices, payer data, callbacks, or return URLs here.
